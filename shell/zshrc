#!/bin/bash

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

# Load our dotfiles like ~/.aliases, etc…
for file in $HOME/.zsh/.{aliases,zsh.local}; do
    [ -r "$file" ] && source "$file"
done
unset file
